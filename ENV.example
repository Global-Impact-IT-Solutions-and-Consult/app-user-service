# Server Configuration
PORT=3000
API_PREFIX=api
NODE_ENV=development

# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/user-service

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=24h

# Encryption Key (for API keys and webhook secrets)
ENCRYPTION_KEY=your-32-character-encryption-key-here

# Search Configuration (MongoDB-based flexible search)
# No external search service needed - uses MongoDB for flexible multi-field search

# Receipt Service Configuration
RECEIPT_SERVICE_URL=http://localhost:3001

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200

# Rate Limiting
THROTTLE_TTL=60000
THROTTLE_LIMIT=100

